version: v1beta11

dependencies:
  - name: edgefarm-core
    source:
      git: https://github.com/edgefarm/edgefarm.core
      subPath: /
      branch: cleanup

commands:
  - name: init
    command: |-
      devspace run edgefarm-core.init
